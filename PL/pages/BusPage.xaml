<Page x:Class="PL.BusPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      xmlns:local="clr-namespace:PL"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
     Title="BusLinePage" Background="White">
    <Page.Resources>
        <DataTemplate x:Key="BusTemplate" DataType="local:Bus">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Right" Width="230">
                <TextBlock Text="{Binding LicenseNumberFormat}" TextAlignment="Right" FontWeight="Bold" FontSize="18" FontFamily="Segoe UI Black"/>
                <TextBlock Text="{Binding Status}" TextAlignment="Right" FontSize="15"/>
            </StackPanel>
        </DataTemplate>
        <CollectionViewSource x:Key="busViewSource" d:DesignSource="{d:DesignInstance {x:Type local:Bus}, CreateList=True}"/>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" MappingMode="RelativeToBoundingBox" StartPoint="0.5,0">
                <GradientStop Color="LightGray" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Label Content="Buses:" Grid.Row="0" Grid.Column="1" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontWeight="Bold" FontSize="30"/>
        <StackPanel Grid.Row="1" Grid.Column="1" x:Name="spStations">
            <StackPanel.Background>
                <LinearGradientBrush EndPoint="0.5,1" MappingMode="RelativeToBoundingBox" StartPoint="0.5,0">
                    <GradientStop Color="LightGray" Offset="1"/>
                    <GradientStop Color="#FFA6C8D1" Offset="0.25"/>
                    <GradientStop Color="#FFD9DCDD" Offset="0.086"/>
                    <GradientStop Color="#FFBBD0D6" Offset="0.18"/>
                    <GradientStop Color="#FFABC9D1" Offset="0.608"/>
                    <GradientStop Color="#FFC7D0D2" Offset="0.46"/>
                </LinearGradientBrush>
            </StackPanel.Background>
            <ComboBox x:Name="cbBuses" ItemsSource="{Binding}" Height="40" Background="#FFBBD0D6" SelectionChanged="cbBuses_SelectionChanged">
                <ComboBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel/>
                    </ItemsPanelTemplate>
                </ComboBox.ItemsPanel>
            </ComboBox>
            <StackPanel>
                <TextBox x:Name="searchBox" FontSize="16" TextChanged="SearchBox_TextChanged" Background="White" BorderBrush="#FF1D2744" Height="30"/>
                <materialDesign:PackIcon Kind="Search" Margin="0,-28,10,5" Width="25" Height="25" HorizontalAlignment="Right" Foreground="Gray"/>
            </StackPanel>
            <ListBox x:Name="lbBuses" IsSynchronizedWithCurrentItem="True" ItemsSource="{Binding}" ItemTemplate="{StaticResource BusTemplate}" BorderBrush="#FF222222" SelectionChanged="stations_SelectionChanged" ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Visible" Height="400" Margin="0,0,0.4,0">
            </ListBox>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="updateButton" Width="145" Margin="0,10,5,10" Background="#FF477AA9" Content="UPDATE BUS" Click="updateButton_Click"/>
            <Button x:Name="deleteButton" Width="145" Margin="5,10" Content="DELETE BUS" Background="#FF477AA9" Click="deleteButton_Click"/>
            <Button x:Name="addButton" Width="145" Margin="5,10,15,10" Content="ADD BUS" Background="#FF477AA9" Click="addButton_Click"/>
        </StackPanel>

        <Rectangle Grid.Column="0" Grid.Row="1" Fill="White" Margin="40,30" RadiusX="10" RadiusY="10" Stroke="#FF3C61CA">
        </Rectangle>
        <Grid x:Name="BusInfoGrid" Grid.Column="0" Grid.Row="1" Margin="60,60,60,45" DataContext="{Binding}">
            <!--<Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>-->
            <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding LicenseNumberFormat}" Margin="10,0" TextAlignment="Right" FontWeight="Bold" FontSize="25" FontFamily="Segoe UI Black"/>
                <TextBlock Text="{Binding Status}" Margin="15,10,15,20" TextAlignment="Right" FontSize="24"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,6,20,6">
                    <TextBlock FontSize="20" TextAlignment="Right" FontFamily="Segoe UI Emoji">
                    <Run Text="{Binding RunningDateFormat}"/>
                    <Run Text=" :תאריך תחילת פעילות "/>
                    </TextBlock>
                    <materialDesign:PackIcon Kind="DateRange" Foreground="#FF477AA9" Width="30" Height="30"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,6,20,6">
                    <TextBlock FontSize="20" TextAlignment="Right" FontFamily="Segoe UI Emoji">
                    <Run Text="{Binding KM}"/>
                    <Run Text=" :'קילומטראז "/>
                    </TextBlock>
                    <materialDesign:PackIcon Kind="Odometer" Foreground="#FF477AA9" Width="30" Height="30"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,6,20,6">
                    <TextBlock FontSize="20" TextAlignment="Right" FontFamily="Segoe UI Emoji">
                    <Run Text="{Binding LastTreatmentFormat}"/>
                    <Run Text=" :תאריך טיפול אחרון "/>
                    </TextBlock>
                    <materialDesign:PackIcon Kind="DateRange" Foreground="#FF477AA9" Width="30" Height="30"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,6,20,6">
                    <TextBlock FontSize="20" TextAlignment="Right" FontFamily="Segoe UI Emoji">
                    <Run Text="{Binding BeforeTreatKM}"/>
                    <Run Text=" :קילומטראז' מאז טיפול אחרון "/>
                    </TextBlock>
                    <materialDesign:PackIcon Kind="Odometer" Foreground="#FF477AA9" Width="30" Height="30"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,6,20,6">
                    <TextBlock FontSize="20" TextAlignment="Right" FontFamily="Segoe UI Emoji">
                    <Run Text="{Binding Fuel}"/>
                    <Run Text=" :דלק "/>
                    </TextBlock>
                    <materialDesign:PackIcon Kind="Fuel" Foreground="#FF477AA9" Width="30" Height="30"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,10" HorizontalAlignment="Center">
                    <Button Margin="10,5" Width="120" Height="45" Background="#FF477AA9" Foreground="White" BorderBrush="{x:Null}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Margin="0,0,5,0" Kind="Travel" Height="30" Width="30"/>
                            <TextBlock Text="נסיעה" VerticalAlignment="Center" Margin="5,0,0,0" FontFamily="Champagne &amp; Limousines" FontSize="18"/>
                        </StackPanel>
                    </Button>
                    <Button Margin="10,5" Width="120" Height="45" Background="#FF477AA9" Foreground="White" BorderBrush="{x:Null}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Margin="0,0,5,0" Kind="Screwdriver" Height="30" Width="30"/>
                            <TextBlock Text="טיפול" VerticalAlignment="Center" Margin="5,0,0,0" FontFamily="Champagne &amp; Limousines" FontSize="18"/>
                        </StackPanel>
                    </Button>
                    <Button Margin="10,5" Width="120" Height="45" Background="#FF477AA9" Foreground="White" BorderBrush="{x:Null}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Margin="0,0,5,0" Kind="FuelPump" Height="30" Width="30"/>
                            <TextBlock Text="תדלוק" VerticalAlignment="Center" Margin="5,0,0,0" FontFamily="Champagne &amp; Limousines" FontSize="18"/>
                        </StackPanel>
                    </Button>


                </StackPanel>

            </StackPanel>
        </Grid>
    </Grid>
</Page>

